

yp.Level = function(w, h) {
    this.board = new yp.Board(w, h);
    this.curLoc = new yp.Loc(0, 0);
}

yp.Level.prototype.move = function(dir) {
    // look ahead
    // 
};

yp.Level.prototype.MoveEast = function(dir) {
};
